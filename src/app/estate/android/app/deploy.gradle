def getIP() {
    return "${projectDir}/ip.bat".execute().text.trim()
}

gradle.projectsEvaluated {

    def currentBundleTask = tasks.create(
            name: "name",
            type: Exec) {

//        println "local ip ${ getIP()}"
    }

    currentBundleTask.dependsOn("assembleRelease")

}
